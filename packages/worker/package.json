{
  "name": "@ondanse/worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "moderate": "node dist/moderate.js",
    "dev": "ts-node-dev --respawn --transpile-only src/cli.ts",
    "test": "echo \"No worker tests yet\"",
    "lint": "echo \"No worker lint configured yet\""
  },
  "dependencies": {
    "@ondanse/shared": "workspace:*",
    "mongodb": "^6.12.0",
    "node-cron": "^3.0.3",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
